n = int(input())
r = int(input())

def fact(x):
    f = 1
    for i in range(2, x + 1):
        f *= i
    return f

nCr = fact(n) // (fact(r) * fact(n - r))
nPr = fact(n) // fact(n - r)

print(nCr)
print(nPr)
